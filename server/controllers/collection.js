const { ErrorHandler, paginateHelper } = require('../Helpers');
const { account: AM } = require('../models');

const createCollection = async (req, res) => {
  try {
    //
  } catch (error) {
    return ErrorHandler(res, error);
  }
};

const getAllCollections = async (req, res) => {
  try {
    //
  } catch (error) {
    return ErrorHandler(res, error);
  }
};

const getCollectionsByDistrict = async (req, res) => {
  try {
    //
  } catch (error) {
    return ErrorHandler(res, error);
  }
};

const getAllCollectors = async (req, res) => {
  try {
    //
  } catch (error) {
    return ErrorHandler(res, error);
  }
};

const getCollectorsByDistrict = async (req, res) => {
  try {
    //
  } catch (error) {
    return ErrorHandler(res, error);
  }
};

const searchCollectors = async (req, res) => {
  try {
    //
  } catch (error) {
    return ErrorHandler(res, error);
  }
};

module.exports = {
  createCollection,
  getAllCollections,
  getCollectionsByDistrict,
  searchCollectors,
  getCollectorsByDistrict,
  getAllCollectors
};
